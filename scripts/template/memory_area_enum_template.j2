#ifndef MEMORY_AREA_ENUMERATIONS_GUARD
#define MEMORY_AREA_ENUMERATIONS_GUARD

/**
* Enumeration of access types for a resource.
*/
enum access_type_e{
    READ_ONLY = 0,
    WRITE_ONLY = 1,
    READ_WRITE = 2,
};
/**
* Enumeration of area indices.
* 
* @enum area_index_e
*
{% for memory_area in memory_areas.values() -%}
* @constant {{ memory_area.desc }}{% if not loop.last %},
{% endif %}{% endfor %}
* @constant AREAS_COUNT The total number of areas.
*/
enum area_index_e{ {% for memory_area in memory_areas.values() %}
    {{ memory_area.name }} = {{ memory_area.index }},{% endfor %}
    AREAS_COUNT,
};

#endif /* MEMORY_AREA_ENUMERATIONS_GUARD */
